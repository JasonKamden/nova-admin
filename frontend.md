# Nova 企业级统一后台管理系统前端设计与开发规范

> 本文档为 `frontend/` 正式前端工程的最终设计与开发规范。
>
> 前端基线：**SoybeanAdmin + Vue 3 + TypeScript + Naive UI**。
>
> 页面、组件、交互、查询布局、表格工具栏、刷新、列设置等后台通用能力，必须优先参考 `soybean-admin-example/` 中现有成熟实现，尤其是
**用户管理页面、角色管理页面、菜单管理页面**。

---

# 1. 文档定位

本规范用于统一 Nova 企业级统一后台管理系统前端的：

- 工程结构
- 页面布局
- 组件使用
- 路由与菜单
- 权限控制
- PLATFORM / TENANT Context
- 多租户切换
- 查询与表格规范
- Drawer / Modal 表单规范
- 数据选择器规范
- 国际化
- 主题与暗黑模式
- 缓存与状态刷新
- 个人中心
- 工作台
- 系统管理页面
- 缓存管理
- 字典管理
- 日志管理
- 消息管理
- 消息中心
- 文件与附件
- 导入导出
- 前后端 API 契约
- 测试与构建验证

本规范只定义正式前端 `frontend/` 的实现要求。

---

# 2. 规则优先级

开发时按以下顺序执行：

1. 用户当前最新明确要求
2. 根目录 `AGENTS.md`
3. 本文档 `frontend.md`
4. `frontend/` 当前正式工程实际版本与结构
5. `soybean-admin-example/` 参考实现
6. 通用前端工程经验

若 `frontend/` 与 `soybean-admin-example/` 的版本、目录、组件 API、路由实现存在差异：

> **必须以 `frontend/` 为准，只借鉴 example 的 UI、交互、布局和实现思路。**

禁止为了与 example 一致而整体覆盖正式前端工程。

---

# 3. 正式工程与参考工程边界

## 3.1 正式前端

```text
frontend/
```

是唯一正式前端工程。

所有正式业务功能最终必须在该工程内实现。

## 3.2 UI 参考工程

```text
soybean-admin-example/
```

只用于参考：

- 页面布局
- 查询区域
- 搜索折叠
- 表格
- 分页
- 刷新
- 列设置
- Drawer
- Modal
- TreeTable
- 用户管理
- 角色管理
- 菜单管理
- Theme Config
- i18n
- Tabs
- KeepAlive
- 权限展示
- 主题样式

禁止直接用 example 的完整：

```text
package.json
src/router
src/store
src/service
src/layout
src/theme
src/locales
```

覆盖 `frontend/`。

---

# 4. 技术定位

前端统一使用：

- Vue 3
- TypeScript
- SoybeanAdmin
- Naive UI
- Vue Router
- Pinia
- Axios 或当前 SoybeanAdmin 实际 Request 封装
- ECharts
- i18n
- 当前 SoybeanAdmin 已集成的表格、Hook、主题、Tabs、KeepAlive、权限体系

前端不得脱离当前 SoybeanAdmin 工程重新搭建另一套 UI 框架。

---

# 5. 命名规则

前端内部目录、组件、Hooks、Store、Service、类型名称保持通用、简洁。

禁止为了品牌化给所有前端对象增加 `nova` 前缀。

推荐：

```text
views/system/user
views/system/role
views/system/menu
views/system/department
views/system/dictionary
views/monitor/cache
views/monitor/operation-log
```

不推荐：

```text
views/nova-system/nova-user
NovaUserTable.vue
useNovaUserTable
```

Department 正式使用完整命名：

```text
Department
department
```

禁止把正式业务命名缩写为：

```text
Dept
dept
```

---

# 6. PLATFORM / TENANT Context

系统存在两个运行 Context：

```text
PLATFORM
TENANT
```

## 6.1 PLATFORM

平台管理不是普通 Tenant。

前端表示：

```text
contextType = PLATFORM
currentTenantId = null
```

不得使用：

```text
tenantId = 0
tenantId = -1
```

表示平台。

## 6.2 TENANT

```text
contextType = TENANT
currentTenantId = 实际租户 ID
```

当前 Tenant 的业务页面由后端 Context 决定数据范围。

前端不得在 Tenant 业务写接口中自行提交一个可篡改的 `tenantId` 作为可信租户来源。

---

# 7. ContextSwitcher

顶部右侧统一提供：

```text
ContextSwitcher
```

不要命名为：

```text
TenantSwitcher
```

因为 PLATFORM 不是 Tenant。

推荐顶部结构：

```text
[第一人民医院 ▼]   [消息]   [头像 张三 ▼]
```

PLATFORM：

```text
[平台管理 ▼]       [消息]   [头像 管理员 ▼]
```

## 7.1 平台管理员

平台管理员可看到：

```text
平台管理
租户 A
租户 B
租户 C
...
```

不要求通过 `sys_user_tenant` 给平台管理员逐一绑定所有 Tenant。

平台管理员切换到 Tenant 后：

- 身份仍然是平台管理员
- 不是模拟租户管理员
- 审计仍记录真实平台管理员

## 7.2 普通用户

- 单 Tenant：自动进入，可隐藏或禁用 ContextSwitcher
- 多 Tenant：允许切换
- 不显示 PLATFORM

## 7.3 大量 Tenant

不得一次性渲染几千个 Tenant。

采用：

```text
最近使用 Tenant
+
远程搜索
```

例如：

```http
GET /api/platform/tenants/options?keyword=...
```

## 7.4 Context 切换后的前端处理

Context 切换成功后必须清理旧上下文相关状态：

- 权限
- 动态菜单
- 动态路由
- Tabs
- KeepAlive
- 页面缓存
- 业务 Store
- 字典缓存
- 参数缓存

随后重新加载：

- 当前用户信息
- 当前 Context
- 菜单
- 按钮权限
- DataScope 相关信息
- 字典
- 参数

最后进入工作台。

严禁跨 Tenant 保留旧 Tenant 的业务 Tabs 和页面缓存。

---

# 8. 顶部导航与登录用户区域

顶部右侧统一按照以下顺序展示：

```text
[ContextSwitcher]   [消息铃铛 Badge]   [头像 + 用户名 ▼]
                                      ├── 个人中心
                                      └── 退出登录
```

TENANT 示例：

```text
[第一人民医院 ▼]   [🔔 8]   [头像 张三 ▼]
```

PLATFORM 示例：

```text
[平台管理 ▼]       [🔔]     [头像 管理员 ▼]
```

登录用户下拉菜单只保留：

```text
头像 + 用户名
├── 个人中心
└── 退出登录
```

不在用户菜单中重复加入：

```text
切换租户
主题设置
角色管理
消息设置
```

Context 切换由 `ContextSwitcher` 负责。

主题由 SoybeanAdmin Theme Config 负责。

消息通知由独立的顶部消息铃铛负责，不放入登录用户下拉菜单。

## 8.1 消息铃铛 Badge

未读数量显示规则固定为：

```text
0       → 不显示 Badge
1 ~ 99  → 显示真实数量
>= 100  → 显示 99+
```

前端 Store 中必须保留真实未读数量，例如实际未读 `286` 时，Header Badge 显示 `99+`，消息中心仍显示真实数量 `286`。

可为 `99+` 提供 Tooltip：

```text
当前有 286 条未读消息
```

---

# 9. 企业后台列表页面统一 UI 基线

## 9.1 最高优先级参考页面

所有“查询 + 表格”后台页面必须优先参考：

```text
soybean-admin-example
→ 用户管理页面
```

用户管理页面作为全项目列表型页面的 UI 基线。

禁止每个业务模块自行设计不同的查询卡、按钮区域、刷新按钮或列设置位置。

## 9.2 统一结构

```text
查询区域
↓
列表区域
```

示意：

```text
┌──────────────────────────────────────────────────────────────┐
│ ˅ 搜索                                                       │
│                                                              │
│ 查询条件 ...                                                 │
│                                            [重置] [搜索]      │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│ XX列表                          [业务按钮] [刷新] [列设置]      │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│                         Table                                │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│                                                   Pagination │
└──────────────────────────────────────────────────────────────┘
```

---

# 10. 查询区域规范

## 10.1 搜索折叠

查询区域左上必须参考用户管理现有样式：

```text
˅ 搜索
```

折叠方式、图标、动画、布局全部优先复用 SoybeanAdmin 用户管理页面现有实现。

不要自行增加文字：

```text
收起
展开
隐藏条件
```

## 10.2 查询表单

查询条件使用用户管理同款：

- Form
- Grid
- Label 宽度
- Input 高度
- Select 高度
- Row Gap
- Column Gap
- 响应式断点

## 10.3 查询按钮

固定参考用户管理：

```text
[重置] [搜索]
```

位置、顺序、尺寸保持一致。

禁止某页面使用：

```text
[搜索] [重置]
```

另一页面又使用：

```text
[重置] [查询]
```

除非正式用户管理基线本身发生统一调整。

---

# 11. 列表工具栏规范

列表 Card 左上显示：

```text
用户列表
角色列表
缓存列表
操作日志
消息列表
```

右上按照用户管理页面布局排列：

```text
[业务按钮...] [刷新] [列设置]
```

例如用户管理：

```text
[新增] [批量删除] [导入] [导出] [刷新] [列设置]
```

缓存管理：

```text
[批量清理] [刷新] [列设置]
```

操作日志：

```text
[导出] [刷新] [列设置]
```

消息管理：

```text
[新增] [批量删除] [刷新] [列设置]
```

刷新、列设置的位置必须和用户管理保持一致。

---

# 12. 列设置规范

统一复用 SoybeanAdmin 当前列设置能力。

应支持：

- 显示/隐藏
- 顺序调整
- 固定列
- 恢复默认

可持久化：

```text
用户 + Route
```

或使用当前 SoybeanAdmin 已有持久化方式。

禁止每个业务页面重新写一套 Column Setting。

---

# 13. 表格规范

表格统一：

- Naive UI / SoybeanAdmin 当前表格组件
- 统一 loading
- 统一 Empty
- 统一 Pagination
- 统一 Row Action
- 统一 Status Tag
- 统一 Tooltip
- 统一溢出省略
- 统一表格高度适配

行操作：

- ≤ 3 个：直接展示
- > 3 个：使用 `更多`

例如：

```text
编辑
详情
更多 ▼
  ├── 删除
  └── 其他操作
```

---

# 14. 表单 Drawer / Modal 全局规范

Theme Config 必须增加统一的 **业务表单打开方式** 配置，用于控制新增、编辑等业务表单采用侧边弹窗还是直接弹窗。

配置项建议显示为：

```text
业务表单打开方式

● 侧边弹窗
○ 直接弹窗
```

内部枚举建议保持简单：

```text
drawer
modal
```

对应关系：

```text
drawer → 侧边弹窗 → NDrawer
modal  → 直接弹窗 → NModal
```

该配置属于 Theme / UI Preference，应复用 SoybeanAdmin Theme Config 的现有配置体系和持久化机制，不单独建立业务配置页面。

同一个业务表单只维护一份。

推荐抽象：

```text
BusinessFormContainer
```

由全局 Theme Config 决定实际容器渲染为 `NDrawer` 或 `NModal`，业务页面不得分别维护 Drawer 和 Modal 两套表单代码。

禁止：

```text
UserDrawerForm
UserModalForm
RoleDrawerForm
RoleModalForm
```

这类成对重复实现。

该全局配置适用于：

- 用户新增 / 编辑
- Department 新增 / 编辑
- 角色新增 / 编辑
- 菜单新增 / 编辑
- 租户新增 / 编辑
- 字典类型新增 / 编辑
- 字典数据新增 / 编辑
- 参数新增 / 编辑
- 消息新增 / 编辑
- 后续业务模块标准新增 / 编辑表单

以下页面不强制受该配置约束：

- 操作日志详情
- 消息只读详情
- 缓存详情
- 阅读情况
- 复杂只读查看器
- 代码 / JSON / Stack Trace 查看器

这些场景可根据阅读体验固定使用 Drawer，尤其操作日志详情继续固定为大尺寸右侧 Drawer。

小屏时 Drawer 应自动适配全宽；Modal 应限制最大高度并提供内部滚动，禁止内容超出可视区域。

---

# 15. 关系选择 UI 规范

统一原则：

```text
简单字段        → Form / Select
多对多关系      → 专用可搜索选择表格
层级关系        → Tree / TreeTable
权限菜单        → Checkbox Tree
```

禁止将大量角色、用户、Department 全部塞入普通 MultiSelect。

---

# 16. 登录页面

登录页面保持 SoybeanAdmin 原有整体视觉体系。

支持：

- 用户名
- 密码
- 验证码（若后端启用）
- Tenant 选择（根据登录流程实际需要）
- 记住登录状态（若后端策略允许）

多租户登录：

- 单 Tenant：自动进入
- 多 Tenant：按后端返回可访问 Tenant 处理
- 平台管理员：默认 PLATFORM

不得通过前端静态写死 Tenant。

---

# 17. 工作台 Dashboard

工作台使用：

```text
/dashboard
```

同一路由根据当前 Context、角色、权限和 DataScope 展示不同数据。

当前正式重点版本采用 **Tenant 管理员工作台** 作为主要设计基线。

## 17.1 Tenant 管理员最终布局

```text
┌──────────────────────────────────────────────────────────────────┐
│ 下午好，张三                                                     │
│ 当前空间：第一人民医院    Department：信息中心                   │
├──────────────┬──────────────┬──────────────┬────────────────────┤
│ 用户总数      │ 部门数量      │ 角色数量      │ 在线用户           │
│    326       │     18       │     12       │      47            │
├─────────────────────────────────┬────────────────────────────────┤
│                                 │                                │
│      最近 7 天登录趋势          │       用户状态分布             │
│                                 │                                │
│           折线图                │           环形图               │
│                                 │                                │
├─────────────────────────────────┼────────────────────────────────┤
│ 最近操作                        │ 快捷入口                       │
│                                 │                                │
│ 张三 修改用户                   │ 用户管理    Department         │
│ 李四 新增部门                   │ 角色管理    文件管理            │
│ 王五 导出数据                   │ 消息中心    操作日志            │
│                                 │                                │
├─────────────────────────────────┴────────────────────────────────┤
│ 通知公告                                              查看更多 > │
│                                                                  │
│ [公告] 系统升级维护通知                              08-08       │
│ [通知] 用户密码安全策略调整                          08-06       │
└──────────────────────────────────────────────────────────────────┘
```

## 17.2 统计卡

第一版：

- 用户总数
- 部门数量
- 角色数量
- 在线用户

## 17.3 图表

第一版：

- 最近 7 天登录趋势
- 用户状态分布

使用 ECharts。

## 17.4 最近操作

显示当前 DataScope 范围内有权限查看的最近操作。

普通用户不得看到其他用户敏感操作。

## 17.5 快捷入口

根据动态菜单和权限生成。

不得静态展示用户无权限进入的入口。

## 17.6 通知公告

展示当前用户可查看的公告、通知。

无数据时显示真实空状态，不得使用静态演示数据。

## 17.7 PLATFORM 工作台

PLATFORM Context 的工作台用于平台统计，例如：

- 租户总数
- 正常租户
- 平台用户
- 今日登录
- 租户增长趋势
- 租户状态分布
- 最近新增租户
- 最近平台操作

跨 Tenant 统计必须调用明确的 `/api/platform/**` 后端接口。

不得把 PLATFORM 理解为“Tenant 列表页自动查询全部租户业务数据”。

---

# 18. 个人中心

个人中心路由建议：

```text
/profile
```

不进入动态 `sys_menu`。

所有已登录用户天然可访问。

## 18.1 最终视觉结构

```text
┌────────────────────────────────────────────────────────────┐
│ 个人中心                                                    │
│ 管理您的个人资料、账户信息和安全设置                         │
├──────────────────┬─────────────────────────────────────────┤
│                  │                                         │
│       ●头像       │  基本资料   安全设置   账号信息           │
│                  │  ───────                               │
│       张三        │                                         │
│     zhangsan     │  头像        [ ● ]                      │
│                  │                                         │
│ ---------------- │  姓名        [张三                    ] │
│ 当前空间          │                                         │
│ 第一人民医院      │  手机号      [138********             ] │
│                  │                                         │
│ 当前部门          │  邮箱        [xxx@example.com         ] │
│ 信息中心          │                                         │
│                  │  性别        [请选择                  ▼] │
│ 当前角色          │                                         │
│ [租户管理员]      │                             [保存修改]   │
│ [业务管理员]      │                                         │
└──────────────────┴─────────────────────────────────────────┘
```

左侧约：

```text
280 ~ 320px
```

右侧自适应。

## 18.2 左侧信息

展示：

- 头像
- 姓名
- 登录账号
- 当前空间
- 当前 Department
- 平台身份
- 当前 Tenant Role
- 账号状态

PLATFORM 时不得显示：

```text
当前租户：null
```

而应显示：

```text
当前空间：平台管理
身份：平台管理员
```

平台管理员切入 Tenant 后，仍显示真实平台身份。

## 18.3 基本资料

允许修改：

- 头像
- 姓名/昵称
- 手机号
- 邮箱
- 性别
- 简介（若系统保留）

只读：

- 用户 ID
- username
- Tenant
- Department
- Role
- 平台管理员身份
- 账号状态

用户不得在个人中心自行改变 Department、Role、Tenant。

## 18.4 安全设置

第一版只实现：

```text
修改密码
```

密码修改成功后退出当前会话并重新登录。

## 18.5 账号信息

使用 `NDescriptions` 展示：

- 用户 ID
- 登录账号
- 状态
- 创建时间
- 最近登录时间
- 最近登录 IP
- 当前 Context
- 当前 Tenant
- 当前 Department
- 当前角色

---

# 19. 用户管理

用户管理是整个系统列表页面 UI 基线之一。

实现：

- 查询
- 分页
- 新增
- 编辑
- 启用/禁用
- 批量操作
- 导入
- 导出
- 用户详情
- 角色分配

用户角色分配不得使用普通 MultiSelect。

必须使用专用选择表格：

- 搜索
- 分页
- Checkbox
- 跨页选择
- 当前已选数量
- 全量替换提交

---

# 20. Department 管理

系统当前组织模型只有：

```text
Tenant
└── Department
    └── Child Department
```

不实现 Organization。

不实现 Post。

Department 页面使用：

```text
TreeTable
```

支持：

- 查询
- 新增
- 编辑
- 删除
- 启用/禁用
- 展开/折叠树
- 排序

字段使用完整 `Department / department` 命名。

---

# 21. 角色管理

角色管理使用标准用户管理列表布局。

支持：

- 角色查询
- 新增
- 编辑
- 删除
- 状态
- DataScope
- 用户分配
- 菜单权限分配

## 21.1 角色菜单权限

使用 Checkbox Tree。

支持：

- 半选
- 父子联动
- 展开全部
- 收起全部
- 全选
- 清空
- 搜索

禁止简单使用树节点 ID 文本输入。

## 21.2 DataScope

前端显示后端定义的数据范围，例如：

```text
ALL
TENANT
DEPARTMENT
DEPARTMENT_AND_CHILDREN
SELF
CUSTOM
```

角色和 DataScope 分开理解。

---

# 22. 菜单管理

菜单管理使用 TreeTable。

支持：

- 目录
- 菜单
- 按钮权限
- Route
- Component
- Icon
- i18nKey
- Permission
- Sort
- Status
- Visible
- Cache

菜单树维护应参考 SoybeanAdmin 现有菜单管理实现。

---

# 23. 租户管理

租户管理只在 PLATFORM Context 显示。

使用用户管理同款列表布局。

支持：

- 查询
- 新增
- 编辑
- 启用/停用
- 查看详情
- 有效期（若后端支持）

租户列表行操作不提供：

```text
进入租户
```

进入/切换 Tenant 统一由顶部 `ContextSwitcher` 完成。

新增 Tenant 成功后：

- 留在 PLATFORM
- 刷新租户列表
- 刷新 ContextSwitcher Options
- 不自动切入新 Tenant

---

# 24. 字典管理

字典管理采用：

```text
可折叠左右分栏
```

左侧：字典类型
右侧：字典数据

## 24.1 最终布局

```text
┌───────────────────────────┬─────────────────────────────────────────────────────┐
│ 字典类型              [+] │ ˅ 搜索                                              │
│                           │                                                     │
│ 🔍 [搜索名称或编码      ] │ 字典标签 [          ]  字典值 [                 ]   │
│                           │ 状态     [全部    ▼]                   [重置][搜索] │
│ 用户性别             3  ⋯ │                                                     │
│ 用户状态             2  ⋯ ├─────────────────────────────────────────────────────┤
│ 是否                 2  ⋯ │ 字典数据 [用户性别]      [新增][批量删除][刷新][列设置]│
│ 消息类型             5  ⋯ │                                                     │
│ 文件类型             8  ⋯ │ □ 序号 标签 值 样式 排序 状态 备注           操作 │
│                           │                                                     │
│                           │ 男   MALE     ...                         编辑 删除 │
│                           │ 女   FEMALE   ...                         编辑 删除 │
│                          ◀│                                                     │
└───────────────────────────┴─────────────────────────────────────────────────────┘
```

## 24.2 左侧折叠按钮

折叠图标位于：

```text
左右分栏分隔线
+
垂直居中
```

展开状态：

```text
◀
```

折叠状态：

```text
▶
```

Tooltip：

```text
收起字典类型
展开字典类型
```

不要显示“收起”“展开”文字按钮。

折叠仅改变布局，不清除当前选中 Dictionary Type。

折叠状态可保存在前端 Local Storage。

## 24.3 左侧宽度

展开建议：

```text
280px
```

折叠后只保留一个窄控制区。

第一版不实现拖动改变宽度。

## 24.4 新增字典类型

左侧标题右侧：

```text
[+]
```

Tooltip：

```text
新增字典类型
```

新增成功后：

- 刷新左侧类型列表
- 自动选中新类型
- 右侧加载该类型数据

## 24.5 修改 / 删除字典类型

字典类型每行：

```text
用户性别     3    ⋯
```

`⋯` 菜单：

```text
编辑
删除
```

不常驻显示两个大按钮。

字典编码创建后原则上只读。

存在字典数据时默认禁止直接删除整个字典类型。

## 24.6 右侧搜索和列表

右侧必须严格参考 SoybeanAdmin 用户管理：

- `˅ 搜索`
- 查询 Grid
- 重置 / 搜索
- 列表标题
- 新增
- 批量删除
- 刷新
- 列设置
- Table
- Pagination

## 24.7 字典数据字段

建议：

- 字典标签
- 字典值
- 标签样式
- 排序
- 状态
- 备注
- 更新时间

标签样式只允许有限枚举：

```text
default
primary
info
success
warning
error
```

不得存任意 CSS。

---

# 25. 参数管理

参数管理使用标准用户管理列表布局。

建议查询：

- 参数名称
- 参数编码
- 参数类型
- 状态

列表：

- 参数名称
- 参数编码
- 参数值摘要
- 类型
- 状态
- 更新时间
- 操作

敏感参数值在列表默认脱敏。

详情或编辑也不得直接展示后端禁止返回的 Secret。

---

# 26. 缓存管理

缓存管理定位：

> 管理系统注册的逻辑业务缓存，不做 Redis Desktop Manager。

不得提供：

- 任意 Redis Key 浏览
- 任意 Value 修改
- Redis 命令执行
- `KEYS *`
- `FLUSHDB`
- `FLUSHALL`

## 26.1 顶部统计

建议：

- Redis 状态
- 缓存区域
- 缓存条目
- 本地缓存区域

## 26.2 查询布局

必须参考用户管理页面：

```text
˅ 搜索
```

查询：

- 缓存名称
- 缓存类型
- 所属模块
- 状态

列表区域：

```text
缓存列表                     [批量清理] [刷新] [列设置]
```

## 26.3 列表字段

- 缓存名称
- 缓存编码
- 类型
- 模块
- Scope
- 条目数
- 命中率
- TTL
- 状态
- 更新时间
- 操作

## 26.4 缓存编码

前端使用逻辑编码：

```text
system.dictionary
system.config
system.menu
security.rolePermission
security.userPermission
system.departmentTree
```

不得依赖具体 Redis Key Prefix。

## 26.5 操作

行操作：

- 详情
- 刷新缓存
- 清理缓存

注意区分：

```text
列表右上刷新
→ 重新请求缓存列表

行内刷新缓存
→ 执行业务缓存失效/重建
```

批量清理必须二次确认。

PLATFORM 全租户高风险清理需要更强确认。

## 26.6 缓存详情

使用 Drawer，只展示：

- 逻辑缓存元数据
- 状态
- 统计
- TTL
- 说明

不展示 Token、Session、完整 Redis Value、Secret。

---

# 27. 在线用户

在线用户属于 Session / Token 管理，不放缓存管理中。

页面使用标准列表布局。

支持：

- 用户查询
- IP
- Tenant
- 登录时间
- 最近活动时间
- 客户端
- 强制下线

强制下线必须确认。

---

# 28. 登录日志

登录日志使用标准用户管理列表布局。

建议查询：

- 用户名
- 登录状态
- IP
- 时间范围

列表：

- 用户
- Tenant / Context
- IP
- 地区（若后端提供）
- 客户端
- 登录状态
- 失败原因
- 登录时间

日志默认只读。

---

# 29. 操作日志

操作日志页面使用标准用户管理列表布局。

## 29.1 查询条件

第一版：

- 操作模块
- 操作类型
- 操作人
- 请求方法
- 操作状态
- 请求 IP
- 操作时间

## 29.2 列表区域

```text
操作日志                                  [导出] [刷新] [列设置]
```

列表建议：

- 序号
- 操作模块
- 操作类型
- 操作描述
- 操作人
- Context
- 请求方法
- 请求地址
- 请求 IP
- 状态
- 耗时
- 操作时间
- 详情

操作列只需要：

```text
详情
```

操作日志普通页面不提供编辑、删除。

---

# 30. 操作日志详情

最终固定为：

> **大尺寸右侧 Drawer + 单页分区**

不使用 Tab。

## 30.1 页面结构

```text
操作日志详情

基本信息
↓
请求信息
↓
响应信息
↓
异常信息
```

Drawer 建议：

```text
75vw 左右
max-width: 1200px ~ 1400px
小屏 100%
```

## 30.2 基本信息

使用 `NDescriptions`。

显示：

- 日志 ID
- 操作模块
- 操作类型
- 操作描述
- 操作状态
- 执行耗时
- 操作用户
- 用户账号
- 用户 ID
- Context
- Tenant
- Department
- 请求 IP
- User-Agent
- Request ID
- Trace ID
- 操作时间

## 30.3 请求信息

显示：

- Method
- URI
- Content-Type
- Query Params
- Path Params
- Request Body
- 白名单 Header（若后端提供）

JSON 必须格式化。

支持复制。

## 30.4 响应信息

显示：

- HTTP Status
- Business Code
- Response Body

JSON 格式化并支持复制。

## 30.5 异常信息

显示：

- Exception Type
- Error Code
- Error Message
- Exception Location
- Stack Trace

Stack Trace：

- 等宽字体
- 保留换行
- 支持复制
- 支持滚动

无异常时显示：

```text
本次操作无异常
```

## 30.6 代码内容区域

可统一复用只读代码展示组件，例如：

```text
LogCodeViewer
```

能力：

- JSON 格式化
- Plain Text
- 等宽字体
- 复制
- 内部滚动
- 长内容展开/收起
- Empty

## 30.7 安全

前端不得要求后端返回未脱敏：

- password
- oldPassword
- newPassword
- token
- accessToken
- refreshToken
- Authorization
- Cookie
- SecretKey
- AccessKey

文件上传日志不得展示文件二进制。

大型请求/响应如果后端截断，应明确显示“内容已截断”。

---

# 31. 消息体系划分

消息体系分为：

```text
消息管理
→ 管理员创建、编辑、发送、撤回、查看阅读情况

消息中心
→ 登录用户查看自己收到的消息、已读/未读
```

两者不要混成一个页面。

---

# 32. 消息管理

消息管理使用标准用户管理列表布局。

## 32.1 查询

- 消息标题
- 消息类型
- 发送状态
- 创建人
- 创建时间

## 32.2 列表

```text
消息列表                     [新增] [批量删除] [刷新] [列设置]
```

字段：

- 消息标题
- 消息类型
- 接收范围
- 状态
- 已读 / 总数
- 创建人
- 创建时间
- 发送时间
- 操作

状态：

```text
DRAFT
SENT
WITHDRAWN
```

有真实定时发送需求后再增加 `SCHEDULED`。

## 32.3 状态与操作

草稿：

```text
编辑
发送
更多 → 删除
```

已发送：

```text
详情
阅读情况
更多 → 撤回
```

已撤回：

```text
详情
阅读情况
```

批量删除只允许草稿。

已发送消息正文和接收范围不可直接修改。

---

# 33. 新增 / 编辑消息

使用大尺寸业务 Drawer / Modal，按全局表单容器规则实现。

字段：

- 消息标题
- 消息类型
- 接收范围
- 富文本内容
- 附件

底部：

```text
[保存草稿] [发送]
```

## 33.1 富文本

必须支持富文本编辑器。

可支持：

- 粗体
- 斜体
- 标题
- 列表
- 链接
- 表格
- 图片（按文件策略）

不得允许危险 HTML 直接执行。

前端渲染已发送富文本必须使用安全处理后的 HTML。

## 33.2 接收范围

第一版：

```text
全部用户
Department
角色
指定用户
```

TENANT Context 中的“全部用户”只表示当前 Tenant 用户。

## 33.3 Department 选择

使用 Department Tree。

可支持：

```text
包含下级 Department
```

## 33.4 角色选择

使用角色选择器，不使用超长 MultiSelect。

## 33.5 指定用户

使用可搜索用户表格：

- 用户名
- 姓名
- Department
- 状态
- 分页
- 跨页选择
- 已选数量

## 33.6 发送确认

点击发送后必须二次确认。

显示：

- 标题
- 类型
- 接收范围
- 预计接收人数

---

# 34. 消息详情

使用大尺寸 Drawer + 单页分区。

展示：

```text
基本信息
接收信息
消息内容
附件
```

基本信息：

- 标题
- 类型
- 状态
- 创建人
- 创建时间
- 发送时间

接收信息：

- 接收范围
- Tenant
- 接收人数
- 已读人数
- 未读人数
- 已读率

消息内容：

- 富文本只读展示

附件：

- 文件名
- 预览
- 下载

---

# 35. 消息阅读情况

点击：

```text
已读数 / 总数
```

或：

```text
阅读情况
```

打开 Drawer。

顶部：

- 接收人数
- 已读人数
- 未读人数
- 已读率

查询区域继续参考用户管理：

- 用户
- Department
- 阅读状态
- 重置
- 搜索

表格：

- 姓名
- username
- Department
- 阅读状态
- 接收时间
- 阅读时间

---

# 36. 消息中心与顶部实时通知

消息中心属于当前登录用户的消息消费能力，并与顶部消息铃铛、统一消息 Store、SSE 实时通知共同组成完整消息消费体系。

入口：

```text
顶部消息铃铛
```

以及允许在侧栏设置：

```text
消息中心
```

## 36.1 顶部消息通知面板

点击顶部消息铃铛时，不直接跳转消息中心，而是打开轻量通知 Popover / Dropdown：

```text
┌────────────────────────────────────────┐
│ 消息通知                    ↻   全部已读 │
├────────────────────────────────────────┤
│ ● 系统升级维护通知                     │
│   系统将于今晚 23:00 进行升级...       │
│   2分钟前                              │
│                                        │
│ ● 密码安全策略调整                     │
│   请及时完善您的账户安全信息...         │
│   20分钟前                             │
│                                        │
│   已读消息标题                         │
│   消息摘要...                          │
│   昨天 16:30                           │
├────────────────────────────────────────┤
│              查看全部消息 >            │
└────────────────────────────────────────┘
```

顶部通知面板默认只展示最近 `10` 条消息，不承担完整分页职责。

右上角必须提供：

```text
刷新
全部已读
```

刷新按钮重新获取：

- 最新未读数量
- 最近消息列表

“全部已读”只作用于当前登录用户在当前合法 Context 下的可读消息。

## 36.2 Badge 规则

顶部铃铛未读数量：

```text
0       → 不显示
1 ~ 99  → 显示真实数量
>= 100  → 显示 99+
```

Header 只压缩视觉展示值，Store 和消息中心必须保留后端返回的真实未读数量。

## 36.3 打开通知面板不自动已读

点击铃铛打开通知面板只表示查看消息摘要，不得把全部消息自动标记为已读。

正确行为：

```text
点击铃铛
→ 展开消息摘要
→ 未读状态保持不变
```

点击具体未读消息时：

```text
标记该消息已读
→ 同步真实 unreadCount
→ Badge 更新
→ 打开消息详情
```

已读接口必须由后端保证幂等。前端可以即时更新 UI，但最终未读数量以后端返回结果为准。

## 36.4 顶部消息详情

点击通知面板中的具体消息后，优先使用只读 Drawer 打开详情，避免用户为了查看一条通知离开当前业务页面。

详情展示：

- 标题
- 类型
- 发布来源 / 发送人（若后端提供）
- 发送时间
- 富文本正文
- 附件

首次打开未读消息时默认标记为已读。

## 36.5 消息中心页面结构

点击“查看全部消息”进入完整消息中心。

推荐：

```text
┌────────────────────────────────────────────────────────────────┐
│ 消息中心                                       [全部已读] [刷新]│
├────────────────────────────────────────────────────────────────┤
│ 全部    未读(8)    已读                                       │
├────────────────────────────────────────────────────────────────┤
│ ● [公告] 系统升级维护通知                         08-08 16:00 │
│          系统将于今晚 23:00 进行升级维护...                   │
│                                                                │
│ ● [通知] 密码安全策略调整                         08-07 10:20 │
│          请及时更新账户密码...                                │
│                                                                │
│   [提醒] 文件处理结果                             08-06 09:15 │
│          您提交的文件已经处理完成...                          │
└────────────────────────────────────────────────────────────────┘
```

消息中心必须显示真实未读数量，例如：

```text
未读(286)
```

不能因为 Header 显示 `99+` 而在消息中心也截断数量。

## 36.6 消息中心筛选

第一版：

```text
全部
未读
已读
```

可以增加消息类型筛选，但不要过度复杂。

支持手动刷新。

## 36.7 消息项

显示：

- 类型 Tag
- 标题
- 摘要
- 时间
- 已读 / 未读状态
- 附件提示（如有）

未读消息应有明显但克制的视觉提示。

点击未读消息后自动标记已读，再打开详情。

## 36.8 全部已读

支持：

```text
全部标为已读
```

执行成功后：

- Header Badge 消失
- 消息通知面板未读标记清除
- 消息中心未读数量更新为真实值 `0`
- 统一消息 Store 同步更新

全部已读属于低风险操作，第一版可以不做二次确认，但必须有成功 / 失败反馈。

## 36.9 SSE 实时消息通知

顶部消息通知必须支持 SSE，用于当前登录用户的消息增量通知。

SSE 只负责实时变化，不负责历史查询、完整分页和完整详情。

统一原则：

```text
REST → 查询当前完整状态
SSE  → 接收增量变化
```

登录成功后的顺序：

```text
登录成功
↓
请求真实未读数量
↓
请求最近消息
↓
建立 SSE 连接
```

禁止登录后只等待 SSE，因为历史未读消息不会自动补齐。

SSE 第一版至少处理：

```text
MESSAGE_CREATED
MESSAGE_WITHDRAWN
UNREAD_COUNT_CHANGED
```

如果后端采用统一事件，也可以通过一个 `MESSAGE_CHANGED` 事件触发重新拉取未读数量和最近消息，但前后端协议必须稳定。

SSE 事件中建议携带最新真实 `unreadCount`，前端优先使用后端返回值，不依赖简单 `+1 / -1` 推断。

## 36.10 SSE 断线、重连与状态校准

SSE 必须支持断线重连。

重新连接成功后必须执行一次状态校准：

```text
SSE reconnect
↓
重新获取 unreadCount
↓
重新获取 recentMessages
```

即使断线期间漏掉事件，也必须通过 REST 恢复正确状态。

后端可发送 Heartbeat 保持连接，Heartbeat 不产生用户可见通知。

## 36.11 SSE 与 ContextSwitcher

Context 切换时消息状态也必须切换。

流程：

```text
关闭旧 Context SSE
↓
完成 Context 切换
↓
清空旧消息临时状态
↓
加载新 Context 未读数量
↓
加载新 Context 最近消息
↓
建立新 Context SSE
```

严禁 Tenant A 的 SSE 在切换到 Tenant B 后继续向当前 UI 注入消息。

PLATFORM / TENANT 的具体消息范围以后端权限和 Context 协议为准。

## 36.12 退出登录

退出登录必须：

- 主动关闭 SSE 连接
- 清空消息通知 Store
- 清空 `unreadCount`
- 清空最近消息
- 再执行用户、权限、Context、动态路由等登录状态清理

不得在用户退出后继续保持消息连接。

## 36.13 SSE 认证安全

如果当前认证依赖自定义 Header，例如 Sa-Token / Authorization Header，必须使用与正式 Request 体系兼容、支持 Header 和自动重连的
SSE 实现方式。

禁止为了使用原生 `EventSource` 简单把长期 Token 放到 URL：

```text
/api/message/sse?token=xxxx
```

因为 URL 可能进入浏览器历史、代理日志和服务器访问日志。

SSE 的最终实现必须遵循当前正式工程认证体系。

## 36.14 统一消息 Store

Header、消息中心和 SSE 不得分别维护多份未读状态。

建议统一消息 Store 至少维护：

```text
unreadCount
recentMessages
connected
loading
```

所有 REST 查询、标记已读、全部已读和 SSE 事件都统一更新这一份 Store。

---

# 37. 文件 / 附件管理

存储支持：

```text
local
minio
```

前端不关心具体底层实现细节，只依赖统一文件 API。

文件管理使用标准列表布局。

支持：

- 上传
- 查询
- 预览
- 下载
- 删除
- 批量删除
- 文件详情

列表可显示：

- 文件名
- 扩展名
- MIME
- 大小
- Storage Type
- 上传人
- 上传时间
- 状态

严禁前端自行拼接 MinIO 内部地址。

预览、下载必须走后端授权或签名流程。

---

# 38. 导入 / 导出

## 38.1 导入

典型流程：

```text
下载模板
↓
选择文件
↓
上传
↓
校验
↓
导入
↓
显示成功/失败统计
↓
下载失败明细（若有）
```

## 38.2 导出

按当前查询条件导出。

大数据导出如果后端采用异步任务，前端展示任务状态并提供完成后下载。

不得在浏览器中一次性加载巨量数据再生成 Excel。

---

# 39. 国际化 i18n

主要 UI 国际化由前端负责。

包括：

- 菜单
- 路由标题
- 页面标题
- 按钮
- 表格列
- 表单 Label
- 提示
- Theme Config
- 状态文案

动态菜单可使用：

```text
i18nKey
```

前端负责翻译。

业务数据名称，例如：

```text
第一人民医院
信息中心
租户管理员
```

属于用户维护的数据，默认不自动翻译。

后端错误处理优先依赖稳定 Error Code，不依赖中文 `message` 做业务分支。

---

# 40. Theme Config / Dark Mode

必须兼容 SoybeanAdmin 现有 Theme Config，不重新建设另一套主题设置系统。

至少保留和兼容：

- Light / Dark
- Theme Color
- Layout
- Tabs
- KeepAlive

并在 Theme Config 中增加：

```text
业务表单打开方式

● 侧边弹窗
○ 直接弹窗
```

对应内部值：

```text
drawer
modal
```

该配置控制系统标准新增 / 编辑业务表单的打开容器：

```text
侧边弹窗 → NDrawer
直接弹窗 → NModal
```

配置应沿用当前 SoybeanAdmin Theme Config 的 Store、持久化方式和 UI 样式。

用户修改后应立即生效，后续打开的新增 / 编辑业务表单使用新的展示方式；不要求强行改变已经处于打开状态的表单。

该配置属于 UI 偏好，可以本地持久化。

业务页面禁止写死不兼容暗色主题的颜色。

例如状态颜色优先使用 Naive UI Theme Token / Type：

```text
success
warning
error
info
primary
```

禁止大量硬编码：

```text
#fff
#000
#18a058
```

覆盖主题体系。

---

# 41. 响应式

系统以桌面企业后台为主。

支持常规响应式：

- 表单 Grid 自适应
- Drawer 小屏全宽
- 双栏页面窄屏纵向排列或侧栏折叠
- 表格横向滚动

不要求第一版做独立移动端产品。

---

# 42. API 契约

前端 API 路径遵循后端：

```text
/api/platform/**
/api/context/**
/api/system/**
/api/business/**
/api/profile/**
```

推荐依赖 Springdoc OpenAPI 作为契约依据。

前端请求类型与后端 DTO 保持一致。

不得依赖 Entity 字段猜接口。

---

# 43. 错误处理

统一 Request 层处理：

- HTTP 错误
- 业务错误码
- Token 失效
- Tenant 失效
- 无权限
- 网络异常
- 超时

401：

```text
清理会话
→ 登录页
```

403：

显示统一权限错误。

Tenant 禁用/过期导致当前 Context 不可用时：

- 清除当前 Tenant Context
- 回退到合法 Context
- 重新加载权限和菜单

具体以后端返回协议为准。

---

# 44. Loading / Empty / Error 状态

所有页面禁止出现：

```text
请求中无 Loading
空白区域
错误后无限 Spinner
```

统一：

- Skeleton / Spin
- Empty
- Error Message
- Retry（适用时）

列表查询失败后保留当前查询条件。

---

# 45. 权限控制

权限包含：

- Route
- Menu
- Button
- DataScope

前端按钮权限用于 UI 隐藏和用户体验。

后端仍必须再次鉴权。

禁止认为：

```text
按钮隐藏 = 安全
```

动态菜单和按钮权限在 Context 切换后必须重新加载。

---

# 46. 缓存与前端本地持久化

允许本地持久化的主要是 UI 偏好，例如：

- Theme
- 表格列设置
- 字典左侧折叠状态
- 页面非敏感布局偏好

不建议长期持久化：

- 跨 Tenant 业务数据
- 权限快照
- Sensitive Profile
- Token 以外的 Session 敏感信息

Context 切换必须主动清理可能造成 Tenant 泄漏的业务缓存。

---

# 47. 页面数据不得使用静态演示数据

正式页面严禁为了“看起来完整”写入：

```text
张三
第一人民医院
326 个用户
87.7% 已读率
```

作为真实业务数据。

上述内容只用于设计说明。

正式实现必须调用真实后端接口。

后端无数据时显示 Empty。

---

# 48. AntV X6 预留

系统可预留：

```text
流程设计
图编辑
```

未来可基于 AntV X6 实现。

当前无明确业务需求时：

- 可以保留技术扩展说明
- 不创建空页面
- 不创建假功能
- 不在菜单中展示不可用入口

---

# 49. 前端代码组织原则

Codex 开发前必须先分析 `frontend/` 当前实际：

- workspace
- package manager
- router
- store
- request
- types
- hooks
- table
- form
- modal
- drawer
- theme
- locale
- tabs
- keep-alive
- permission

然后再决定文件位置。

不要仅根据本文档示例目录强行改变现有工程。

页面应合理拆分组件，但禁止过度拆分。

例如 Dashboard 可按真实复杂度拆：

```text
welcome
statistics
login-trend
user-status
recent-operations
quick-entry
announcements
```

没有真实使用的模块不要创建空组件。

---

# 50. 公共组件复用

优先复用 SoybeanAdmin 现有公共能力。

可在真实重复需求出现时抽象：

- BusinessFormContainer
- UserSelector
- RoleSelector
- DepartmentTreeSelector
- LogCodeViewer
- FileUploader
- FilePreview

不要为单个页面提前创建大量 Generic Component。

---

# 51. 前端安全

重点验证：

- XSS
- 富文本 XSS
- 文件预览风险
- Token 泄漏
- URL 注入
- Open Redirect
- 越权入口
- Tenant 数据残留
- Sensitive Data 展示
- 日志参数脱敏

富文本必须安全渲染。

禁止使用未经处理的危险 `v-html` 内容。

---

# 52. 性能要求

避免：

- 首屏加载全量 Tenant
- 一次加载全量用户
- 一次加载全量日志
- 大量无必要并发请求
- Watch 深层大对象
- 重复加载相同字典
- Context 切换后保留旧业务缓存

列表统一分页。

大选择器采用远程搜索或分页。

---

# 53. 测试与验证

前端功能完成后至少验证：

1. 安装依赖成功
2. TypeScript 检查成功
3. Lint 成功（若项目配置存在）
4. Build 成功
5. Login 流程正常
6. PLATFORM / TENANT 切换正常
7. 动态路由正常
8. 菜单权限正常
9. Button 权限正常
10. Tabs / KeepAlive Context 切换后无旧 Tenant 残留
11. CRUD 页面查询、分页、重置正常
12. Drawer / Modal 表单正常
13. 用户角色选择正常
14. Role Menu Tree 正常
15. Department Tree 正常
16. 字典左右布局及折叠正常
17. 缓存管理危险操作确认正常
18. 操作日志详情完整展示正常
19. 消息发送、撤回、阅读情况正常
20. 消息中心已读/未读正常
21. 顶部消息铃铛 Badge 0 / 1~99 / 99+ 显示正常
22. 消息通知刷新、单条已读、全部已读正常
23. SSE 首次连接、实时推送、断线重连、重连状态校准正常
24. Context 切换后旧 SSE 已关闭且无跨 Tenant 消息残留
25. 退出登录后 SSE 已关闭且消息 Store 已清理
26. Theme Config 的侧边弹窗 / 直接弹窗切换正常
27. 标准新增 / 编辑业务表单可以根据 Theme Config 使用 Drawer / Modal 且不重复实现表单
28. 文件上传、预览、下载正常
29. i18n 正常
30. Dark Mode 正常
31. Responsive 正常

禁止只执行：

```text
创建文件
```

然后声称“已完成”。

---

# 54. 禁止事项

前端禁止：

- 用 example 覆盖正式 frontend
- 自建另一套 UI Framework
- 页面之间使用不同查询布局
- 页面之间随意改变刷新、列设置位置
- 使用 MultiSelect 选择大量用户/角色
- 使用静态假业务数据
- 跨 Tenant 保留旧页面缓存
- 前端自行信任 tenantId
- 把 PLATFORM 当 tenantId=0
- 新建 Organization 页面
- 新建 Post 页面
- 使用 Dept/dept 作为正式业务命名
- 任意浏览 Redis Key/Value
- 在缓存页面提供 Redis 命令行
- 在日志详情展示密码、Token、Secret
- 未清洗富文本直接 `v-html`
- 创建大量空页面、空组件、TODO 占位
- 创建 `.editorconfig`

---

# 55. 当前正式页面清单

## 公共

```text
登录
工作台
个人中心
```

## PLATFORM

```text
租户管理
平台工作台
平台日志/统计（按真实需求）
```

## 系统管理

```text
用户管理
Department 管理
角色管理
菜单管理
字典管理
参数管理
消息管理
```

## 系统监控

```text
在线用户
登录日志
操作日志
缓存管理
```

## 个人业务

```text
消息中心
```

## 文件能力

```text
文件管理
附件上传/预览/下载
```

## 扩展

```text
导入导出
AntV X6（有真实业务后开发）
```

---

# 56. 最终核心 UI 原则

整个 Nova 前端必须保持以下一致性：

1. **正式工程只认 `frontend/`。**
2. **`soybean-admin-example/` 是 UI 与交互参考，不是覆盖源。**
3. **所有查询列表页面优先参考 example 的用户管理页面。**
4. **搜索折叠、查询 Grid、重置/搜索、业务按钮、刷新、列设置、Table、Pagination 全项目统一。**
5. **PLATFORM / TENANT 是 Context，不把平台伪装成 Tenant。**
6. **Tenant 即业务单位，当前不再增加 Organization。**
7. **Department 使用完整命名，不实现 Post。**
8. **业务表单复用一份，由 Theme Config 控制 Drawer / Modal。**
9. **多对多使用专用选择器，层级数据使用 Tree，权限使用 Checkbox Tree。**
10. **Dashboard 使用真实数据，不做装饰性大屏。**
11. **个人中心保持“左侧用户卡 + 右侧 Tab”的简洁设计。**
12. **字典管理采用可折叠左右分栏，折叠图标位于分隔线垂直居中。**
13. **缓存管理只管理逻辑业务缓存，不做 Redis 运维控制台。**
14. **操作日志详情使用大尺寸右侧 Drawer + 单页分区。**
15. **消息管理负责发布和追踪，消息中心负责用户消费。**
16. **顶部消息铃铛通过统一消息 Store + REST + SSE 实现实时通知，>=100 的未读 Badge 只显示 `99+`。**
17. **打开消息通知面板不自动已读，点击具体消息时默认标记该消息已读。**
18. **Theme Config 必须提供“侧边弹窗 / 直接弹窗”业务表单展示配置，同一个业务表单只维护一份。**
19. **富文本、日志、文件、权限、多租户和 SSE 都必须考虑安全边界。**
20. **没有真实后端能力时，不创建假功能和假数据。**
21. **功能完成后必须真实构建、运行和验证。**

---

# 57. Codex 前端开发执行要求

Codex 开始前端开发时必须执行：

```text
1. 读取 AGENTS.md
2. 读取 frontend.md
3. 检查 frontend/ 当前真实工程
4. 检查 soybean-admin-example/ 对应参考页面
5. 以 frontend/ 的技术版本和目录为准
6. 优先复用现有组件、Hook、Theme、Table、Form、Modal、Drawer
7. 开发真实 API 页面
8. 执行依赖安装、类型检查、构建
9. 修复所有真实错误
10. 验证登录、Context、菜单、权限、CRUD、核心页面交互
```

对于所有列表页面，Codex 必须先查看：

```text
soybean-admin-example 的用户管理页面
```

确认以下实现后再开发：

```text
搜索折叠
查询 Grid
重置 / 搜索
列表标题
业务按钮
刷新
列设置
Table
Pagination
```

禁止仅凭文字描述自行创造另一套布局。

---

# 58. 当前版本结论

当前前端正式设计已经确定以下核心页面方案：

```text
个人中心
→ 左侧用户信息卡 + 右侧基本资料/安全设置/账号信息

工作台
→ Tenant 管理员统计卡 + 登录趋势 + 用户状态 + 最近操作 + 快捷入口 + 通知公告

缓存管理
→ 状态卡 + 用户管理同款查询/列表 + 逻辑缓存管理

字典管理
→ 可折叠左侧字典类型 + 右侧用户管理同款查询/字典数据列表

操作日志
→ 用户管理同款查询/列表 + 详情按钮

操作日志详情
→ 大尺寸右侧 Drawer + 基本信息/请求信息/响应信息/异常信息单页分区

消息管理
→ 管理员创建/编辑/发送/撤回/阅读追踪

消息中心
→ 当前用户全部/未读/已读消息消费 + 顶部消息通知 + SSE 实时更新

顶部消息铃铛
→ 最近 10 条通知 + 刷新 + 全部已读 + 0/1~99/99+ Badge + 点击消息自动已读

Theme Config
→ 新增“业务表单打开方式”，统一支持侧边弹窗 Drawer / 直接弹窗 Modal
```

后续业务模块页面应继续沿用本规范，而不是重新建立新的视觉体系。
